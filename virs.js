var compteurImage = 1;
var totalimage = 10;

function slider(x) {

    var image = document.getElementById('img');
    compteurImage = compteurImage + x;
    image.src = "images4"
    compteurImage + ".jpg";

    if (compteurImage >= totalimage) {
        c
        compteurImage = 1;
    }
    if (condition) {

    }
}